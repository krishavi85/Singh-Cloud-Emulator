name: Security and platform checks

on:
  pull_request:
  push:
    branches: [main, top-tier-production-completion]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm install --ignore-scripts --no-fund
      - run: npm run check
      - run: npm test
      - run: npm audit --omit=dev --audit-level=high
      - run: cp deploy/top-tier.env.example deploy/top-tier.env
      - name: Validate Compose stacks
        env:
          CODE_SERVER_PASSWORD: ci-value
          TURN_REALM: emulator.example.com
          TURN_USERNAME: ci-value
          TURN_PASSWORD: ci-value
          TURN_EXTERNAL_IP: 203.0.113.10
          POSTGRES_PASSWORD: ci-value
          REDIS_PASSWORD: ci-value
          MINIO_ROOT_USER: ci-value
          MINIO_ROOT_PASSWORD: ci-value
          GRAFANA_ADMIN_PASSWORD: ci-value
          WORKER_API_BASE: https://emulator.example.com
          BUILD_WORKER_API_KEY: ci-value
          CAPTURE_WORKER_API_KEY: ci-value
        run: |
          docker compose -f compose.equivalence.yml config >/dev/null
          docker compose -f compose.workers.yml config >/dev/null
          docker compose -f compose.top-tier.yml config >/dev/null
